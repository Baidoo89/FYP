# Troubleshooting & FAQ

- **DB errors:** Ensure `DATABASE_URL` is correct and migrations are applied
- **Auth issues:** Check password hashing and JWT/session config
- **Deployment issues:** Verify environment variables and build logs
- **Schema drift:** Run a full DB reset and migration
- **Need help?** Check logs, audit trails, and code comments